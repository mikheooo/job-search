
# Secrets
.env
*.key
*.pem
twitter_env.sh

# Caches & Envs
__pycache__/
*.pyc
.venv/
venv/
node_modules/

# Logs & Data
*.log
*.sqlite
*.db
*.json
!package.json
!package-lock.json
!vacancies.json

# Browser artifacts (runtime screenshots/state; tracked fixture exceptions below)
artifacts/*
!artifacts/hh_manual_form_snapshot.json
# Exported row deleted from the production state.db on 2026-09-17. Kept as a
# tracked fixture because docs/ble001_triage.md cites this row as evidence that
# the "live read -> db -> live read" path converges (findings #27 and #38).
!artifacts/deleted_quest_1c2be2caaff3bfff.json


# HH authenticated session storage (NEVER commit)
hh_storage_state*.json
*storage_state*.json

# Temporary / scratch (never commit)
_tmp_*
*.bak
*.bak2
*.backup
*.new
graphify-out/
scratch_*


# Disaster-recovery snapshots (local only, contain env/db copies)
baseline_stage14_snapshot/
snapshot_stage15_current/

# Candidate profile TEMPLATE exception (real candidate_profile.json stays local via *.json)
!candidate_profile.example.json

# DR snapshot manifests (local sha256 hashes of snapshot contents)
baseline_stage14_manifest.txt
snapshot_stage15_manifest.txt

# Personal resume (not part of the product)
resume.md
